@page "/"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
    // Use forceLoad to perform a full page load which avoids the WebAssembly
    // JSInterop serialization path that can trigger NullabilityInfoContext_NotSupported
    Navigation.NavigateTo("/portfolio", forceLoad: true);
    }
}
