@page "/optimizer"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
    // Navigate with forceLoad to bypass SPA navigation which triggers problematic
    // System.Text.Json behavior in some WebAssembly environments
    Navigation.NavigateTo("/portfolio", forceLoad: true);
    }
}
